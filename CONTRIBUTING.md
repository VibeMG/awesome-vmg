# 贡献参考或工程

目录保存实际可用的信息，条目无需符合固定作品长度、互动强度、镜头数量或视觉效果门槛。默认推荐标记不是质量等级，不设主观评分、淘汰或审美返工流程。

## 添加外部参考

在 `catalog.json.references` 中添加 ID、标题、类型、来源 URL、关联 `style_ids` 和 `entry_path`；在 `references/` 中写具体入口、可借鉴之处和实际查看范围。案例可以来自网页、代码、影片、AE 预览、图片等媒介，不限定为现有类型。

尽量保留原作者或制作方入口。没有看过视频时，记录为介绍页或检索线索；不要将网页可访问写成整个工程已运行。第三方作品默认只保留链接与说明，复制源码或媒体时再遵循相应授权。

## 添加方向

`catalog.json.styles` 中维护稳定 ID、名称、所属家族、具体画面描述、默认推荐标记、代表参考及说明位置。`description` 也是 Agent 提问时的依据，允许按对话语言翻译；用户始终可以给出目录外的方向。

风格条目可以组合。用途、运动与材质是独立描述，避免把每种组合都再建为互斥分类。英文名称是翻译辅助，不构成另一套分类。

## 添加完整 VMG 工程

按 [标准工程说明](projects/README.md) 提供真实源码入口后，加入 `catalog.json.projects`，标记 `kind: vmg-project`。保留工程自身的依赖和版本记录，复用现有 VMG 工程格式；无需先为本库开发模板管理器或把场景拆成通用组件。

特殊用途工程可以使用空`style_ids`，同时在说明中写明具体用途。提交本地`.vmg`时使用`package_path`和`package_sha256`，附`preview_path`并记录实际验证范围。

## 基础检查

```sh
node scripts/check-catalog.mjs
python3 scripts/check-project-packages.py
```

目录检查覆盖JSON结构、必要字段、索引关联、本地路径安全及包校验值；源码包检查覆盖归档清单、文件与素材哈希及预览来源。不访问外部网站、不执行被引用工程，也不判断内容风格或作品质量。工程的实际打开与编辑情况在自己的说明中如实记录。

提交 PR 时说明新增或修改的条目及观察范围即可。已有 ID 尽量保持稳定，维护一个文件位置时同步更新相应索引。
